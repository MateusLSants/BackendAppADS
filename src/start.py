def soma(num_a: int, num_b: int) -> int:
    """Soma de dois numeros
    :param - num_a: primeiro numero da soma
           - num_b: segundo numero da soma
    :return - soma entre dois numeros
    """

    num_c = num_a + num_b
    return num_c
